---
phase: 04-polish-launch
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - CHAT-05
  - CHAT-14

must_haves:
  truths:
    - "No API key or system prompt visible in browser DevTools network tab during live chat"
    - "All polish features work correctly on the production Netlify URL"
    - "Chat animation, chips, loading screen, hero animation, and back-to-top all function end-to-end"
  artifacts: []
  key_links: []
---

<objective>
End-to-end production validation — verify all Phase 4 features work on the live Netlify deployment and confirm API key security.

Purpose: Final gate before declaring v1 complete. Ensures nothing is broken on production and the API key remains hidden.
Output: Human-verified confirmation that all polish features work on nathanrubin617.com.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-launch/04-CONTEXT.md
@.planning/phases/04-polish-launch/04-01-SUMMARY.md
@.planning/phases/04-polish-launch/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated code verification checks</name>
  <files></files>
  <read_first>
    - chat.js (verify animation and chip code present)
    - styles.css (verify all new CSS rules present)
    - polish.js (verify loading screen, hero animation, back-to-top logic)
    - index.html (verify overlay HTML, hero IDs, back-to-top button, polish.js script)
    - book.html (verify overlay HTML, back-to-top button, polish.js script)
    - basketball.html (verify overlay HTML, back-to-top button, polish.js script)
    - contact.html (verify overlay HTML, back-to-top button, polish.js script)
  </read_first>
  <action>
Run a comprehensive grep-based verification across all files to confirm every Phase 4 feature is wired correctly. Check for:

**CHAT-14 (animation):**
- `styles.css` contains `translateY(20px)` and `transition: opacity 0.2s ease, transform 0.2s ease`
- `chat.js` contains `requestAnimationFrame` and `transitionend` and `transitioning`

**CHAT-05 (chips):**
- `styles.css` contains `.chat-chip` with `border-radius: 999px`
- `chat.js` contains `showChips` and `removeChips`
- `chat.js` contains all 3 chip text strings

**Loading screen (D-10 through D-13):**
- `styles.css` contains `.loading-overlay` and `pulse-logo`
- `polish.js` contains `loading-overlay` and `fade-out`
- All 4 HTML files contain `id="loading-overlay"` and `polish.js`

**Hero animation (D-14 through D-16):**
- `index.html` contains `hero-heading` and `hero-subtext` and `hero-animate-ready`
- `polish.js` contains `hero-animate-visible`
- Other 3 HTML files do NOT contain `hero-heading`

**Back-to-top (D-17 through D-20):**
- `styles.css` contains `.back-to-top` and `.back-to-top.visible`
- `polish.js` contains `scrollY > 300` and `scrollTo`
- All 4 HTML files contain `id="back-to-top"`

**No regressions:**
- `chat.js` still contains `sendMessage` function
- `chat.js` still contains `addAssistantMessage` and `addUserMessage`
- `chat.js` still contains `showTypingIndicator` and `removeTypingIndicator`
- All 4 HTML files still contain `chat-bubble` and `chat-window` and `chat.js`

Write results to `.planning/phases/04-polish-launch/04-VERIFICATION.md`.
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && grep -c "translateY(20px)" styles.css && grep -c "chat-chip" styles.css && grep -c "loading-overlay" styles.css && grep -c "back-to-top" styles.css && grep -c "showChips" chat.js && grep -c "removeChips" chat.js && grep -c "requestAnimationFrame" chat.js && grep -c "loading-overlay" polish.js && grep -c "hero-animate-visible" polish.js && grep -c "scrollY > 300" polish.js && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - All grep checks above return count >= 1
    - .planning/phases/04-polish-launch/04-VERIFICATION.md exists with results
    - No check returns 0 (which would indicate a missing feature)
  </acceptance_criteria>
  <done>All automated code checks pass, confirming every Phase 4 feature is present in the codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production end-to-end verification</name>
  <files></files>
  <read_first>
    - .planning/phases/04-polish-launch/04-CONTEXT.md (decisions D-21, D-22 for validation checklist)
  </read_first>
  <action>
Present the verification checklist to the user for manual testing on the live Netlify URL (nathanrubin617.com). This is a human-verify checkpoint — Claude cannot test the production deployment visually. The user must deploy (push to GitHub or `netlify deploy --prod`) and then walk through each check below.
  </action>
  <what-built>
    All Phase 4 polish features: chat panel animation (CHAT-14), starter question chips (CHAT-05), loading screen overlay, hero text animation, and back-to-top button.
  </what-built>
  <how-to-verify>
    Deploy to Netlify (push to GitHub or run `netlify deploy --prod`) and verify on the live URL (nathanrubin617.com):

    **1. Loading Screen (all pages)**
    - Visit nathanrubin617.com — loading screen with NR logo (pulsing) appears briefly, then fades out
    - Visit /book.html, /basketball.html, /contact.html — same behavior

    **2. Hero Animation (index.html only)**
    - On nathanrubin617.com — after loading screen fades, hero heading "Nathan Rubin" slides up + fades in, then subtext follows slightly after

    **3. Back-to-Top (all pages)**
    - Scroll down 300px on any page — blue circle button appears bottom-left
    - Click it — page smooth-scrolls to top
    - Button disappears when near top

    **4. Chat Animation (CHAT-14)**
    - Click chat bubble — panel slides up + fades in smoothly (not an instant jump)
    - Click X — panel slides down + fades out

    **5. Starter Chips (CHAT-05)**
    - Open chat — 3 blue-outlined chips appear below greeting: "Tell me about his book", "Basketball career", "How to contact him"
    - Click any chip — that text is sent as your message, chips disappear
    - Close and reopen — chips do NOT reappear

    **6. API Key Security (D-21, D-22)**
    - Open DevTools Network tab
    - Send a chat message
    - Inspect the request to `/.netlify/functions/chat` — confirm NO `ANTHROPIC_API_KEY` in headers or body
    - Inspect the response — confirm NO system prompt text in the response body

    **7. Mobile**
    - Test chat and back-to-top on mobile or mobile emulator
    - Back-to-top (bottom-left) and chat bubble (bottom-right) do not overlap
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated command"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 7 verification sections pass on the live production URL
    - User types "approved" to proceed
  </acceptance_criteria>
  <done>Human has verified all Phase 4 features on the live Netlify production URL and confirmed API key security.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 4 requirements verified both via automated grep checks and human production testing.
</verification>

<success_criteria>
- All automated verification checks pass (grep counts > 0 for every feature)
- Human confirms all features work on production Netlify URL
- API key confirmed hidden in DevTools network inspection
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-launch/04-03-SUMMARY.md`
</output>
