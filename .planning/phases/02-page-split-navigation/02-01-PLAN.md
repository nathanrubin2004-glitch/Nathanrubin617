---
phase: 02-page-split-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles.css
  - index.html
  - book.html
  - basketball.html
  - contact.html
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
  - SITE-05
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "All 4 pages load styles from a shared styles.css file instead of inline style blocks"
    - "Each page's nav link for its own page is visually highlighted with blue text and semibold weight"
    - "All original content, images, and Imgur URLs remain in the correct files unchanged"
    - "Nav bar is present and functional on all 4 pages on both desktop and mobile"
    - "Mobile hamburger button has an accessible aria-label"
  artifacts:
    - path: "styles.css"
      provides: "Shared CSS extracted from inline style blocks"
      contains: "--color-primary: #3b82f6"
    - path: "index.html"
      provides: "Home/About page with hero-specific inline styles preserved"
      contains: "href=\"styles.css\""
    - path: "book.html"
      provides: "Book page with shared CSS link"
      contains: "href=\"styles.css\""
    - path: "basketball.html"
      provides: "Basketball page with shared CSS link"
      contains: "href=\"styles.css\""
    - path: "contact.html"
      provides: "Contact page with shared CSS link"
      contains: "href=\"styles.css\""
  key_links:
    - from: "index.html"
      to: "styles.css"
      via: "link rel=stylesheet"
      pattern: "href=\"styles.css\""
    - from: "book.html"
      to: "styles.css"
      via: "link rel=stylesheet"
      pattern: "href=\"styles.css\""
    - from: "basketball.html"
      to: "styles.css"
      via: "link rel=stylesheet"
      pattern: "href=\"styles.css\""
    - from: "contact.html"
      to: "styles.css"
      via: "link rel=stylesheet"
      pattern: "href=\"styles.css\""
---

<objective>
Extract shared CSS into styles.css and add active nav indicators to all 4 pages.

Purpose: Eliminate ~280 lines of duplicated CSS across 4 pages, and give visitors a clear visual indicator of which page they are on (per D-01 through D-05).
Output: A new styles.css file at repo root, all 4 HTML pages updated to link it, active nav classes hardcoded per page, aria-label on mobile menu button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-split-navigation/02-CONTEXT.md
@.planning/phases/02-page-split-navigation/02-RESEARCH.md
@.planning/phases/02-page-split-navigation/02-UI-SPEC.md

@index.html
@book.html
@basketball.html
@contact.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared CSS into styles.css and update all page heads</name>
  <files>styles.css, index.html, book.html, basketball.html, contact.html</files>
  <read_first>
    - basketball.html (canonical source for shared CSS — lines 15-277 contain the shared style block with no page-specific rules)
    - index.html (lines 15-263 contain shared + hero-specific styles — hero rules must stay inline)
    - book.html (confirm style block matches basketball.html)
    - contact.html (confirm style block matches basketball.html)
    - .planning/phases/02-page-split-navigation/02-RESEARCH.md (CSS extraction pattern, pitfalls, load order)
    - .planning/phases/02-page-split-navigation/02-UI-SPEC.md (CSS Extraction Contract section)
  </read_first>
  <action>
    **Step 1: Create styles.css**
    - Copy the entire content between `<style>` (line 15) and `</style>` (line 277) from basketball.html into a new file `styles.css` at repo root.
    - Do NOT include the `<style>` and `</style>` tags themselves — just the CSS rules inside.
    - The file should start with `:root { --color-primary: #3b82f6; ...}` and end with the last chat widget rule.
    - Add a comment at the top: `/* Shared styles — extracted from inline style blocks (Phase 2) */`

    **Step 2: Update book.html, basketball.html, contact.html**
    For each of these 3 files:
    - Remove the entire `<style>...</style>` block (lines 15 through 277 inclusive).
    - In its place, add: `<link rel="stylesheet" href="styles.css">`
    - The `<link>` tag must appear AFTER the Tailwind CDN `<script src="https://cdn.tailwindcss.com"></script>` tag (line 13). Per D-04, D-05.
    - These 3 pages have NO page-specific CSS — the entire style block is removed.

    **Step 3: Update index.html**
    - index.html has hero-specific CSS rules that do NOT appear in the other pages. These must remain inline.
    - Remove the shared CSS rules from the `<style>` block (the rules that are identical to basketball.html's style block).
    - Add `<link rel="stylesheet" href="styles.css">` AFTER the Tailwind CDN script tag (line 13).
    - Keep a residual `<style>` block AFTER the `<link>` tag containing ONLY these hero-specific rules:
      ```css
      /* Hero Styles — index.html only */
      .hero-bg {
          background-image: url('https://i.imgur.com/hnJz5s5.jpeg');
          background-size: cover;
          background-repeat: no-repeat;
          background-position: center;
          position: relative;
          color: white;
          z-index: 1;
      }
      .hero-bg::before {
          content: '';
          position: absolute;
          top: 0; left: 0;
          width: 100%; height: 100%;
          background-color: rgba(0, 0, 0, 0.5);
          border-radius: 0.75rem;
          z-index: -1;
      }
      .social-icons-container {
          position: absolute;
          bottom: 1.5rem; left: 1.5rem;
          z-index: 2;
          display: flex;
          gap: 0.75rem;
      }
      .social-icon:hover { transform: scale(1.1); }
      .bottom-right-logo {
          position: absolute;
          bottom: 1.5rem; right: 1.5rem;
          z-index: 2;
      }
      ```
    - The correct `<head>` order after the Tailwind script is: `<link>` to styles.css, then residual `<style>` block.

    **Load order (critical — per RESEARCH.md Pitfall 3):**
    ```html
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="styles.css">
    <!-- index.html only: residual <style> block here -->
    ```
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && test -f styles.css && grep -q '\-\-color-primary: #3b82f6' styles.css && grep -q 'href="styles.css"' index.html && grep -q 'href="styles.css"' book.html && grep -q 'href="styles.css"' basketball.html && grep -q 'href="styles.css"' contact.html && grep -q 'hero-bg' index.html && echo "PASS: CSS extraction complete" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - styles.css exists at repo root and contains `--color-primary: #3b82f6`
    - styles.css contains `.nav-glass` rule
    - styles.css contains `::-webkit-scrollbar` rules
    - styles.css does NOT contain `.hero-bg` (hero-specific stays inline)
    - All 4 HTML files contain `href="styles.css"`
    - book.html, basketball.html, contact.html do NOT contain `<style>` tags (all CSS moved to shared file)
    - index.html contains a `<style>` block with `.hero-bg` (hero-specific rules preserved)
    - index.html `<link rel="stylesheet" href="styles.css">` appears AFTER `cdn.tailwindcss.com` script
    - index.html `<style>` block (hero rules) appears AFTER `href="styles.css"` link
  </acceptance_criteria>
  <done>All 4 pages link to styles.css. Shared CSS is extracted. Hero-specific styles remain inline on index.html only. Load order is Tailwind CDN -> styles.css -> residual inline.</done>
</task>

<task type="auto">
  <name>Task 2: Add active nav indicators and aria-label to all 4 pages</name>
  <files>index.html, book.html, basketball.html, contact.html</files>
  <read_first>
    - index.html (after Task 1 edits — read the nav section around lines 270-295 to find current nav link classes)
    - book.html (nav section)
    - basketball.html (nav section)
    - contact.html (nav section)
    - .planning/phases/02-page-split-navigation/02-UI-SPEC.md (Nav Active Indicator Contract — exact class strings)
  </read_first>
  <action>
    **Per D-01, D-02, D-03:** Add hardcoded active nav classes to each page's own nav link. No JS detection.

    **Desktop nav link changes (per UI-SPEC):**

    Inactive state (leave as-is on non-current links):
    `class="text-gray-600 hover:text-blue-600 transition-colors"`

    Active state (apply to the current page's link):
    `class="text-blue-600 font-semibold transition-colors"`

    Changes on active link: remove `text-gray-600`, remove `hover:text-blue-600`, add `text-blue-600`, add `font-semibold`.

    **Mobile nav link changes (per UI-SPEC):**

    Inactive state (leave as-is on non-current links):
    `class="block px-8 py-2 font-bold text-gray-800 hover:text-blue-600"`

    Active state (apply to the current page's link):
    `class="block px-8 py-2 font-bold text-blue-600"`

    Changes on active link: remove `text-gray-800`, remove `hover:text-blue-600`, add `text-blue-600`.

    **Active link map (which link gets active classes on which page):**

    | Page | Link text to make active |
    |------|--------------------------|
    | index.html | "Home" (href="index.html") |
    | book.html | "Chasing a Dream" (href="book.html") |
    | basketball.html | "Basketball" (href="basketball.html") |
    | contact.html | "Contact" (href="contact.html") |

    Process ONE page at a time. On each page, modify BOTH the desktop nav link AND the mobile nav link for that page's own link. Leave all other links in their inactive state. Do NOT change any `href` values.

    The "Get the Book" CTA button (`bg-blue-600 text-white px-4 py-2 rounded-full` or similar) receives NO active treatment on any page.

    **Aria-label (per UI-SPEC Interaction Contract):**
    On ALL 4 pages, find the mobile menu button:
    `<button id="mobile-menu-btn" class="lg:hidden p-2 text-gray-600 hover:text-blue-600 focus:outline-none">`

    Add `aria-label="Open navigation menu"` to this button element:
    `<button id="mobile-menu-btn" aria-label="Open navigation menu" class="lg:hidden p-2 text-gray-600 hover:text-blue-600 focus:outline-none">`
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && grep -q 'href="index.html".*text-blue-600 font-semibold\|text-blue-600 font-semibold.*href="index.html"' index.html && grep -q 'href="book.html".*text-blue-600 font-semibold\|text-blue-600 font-semibold.*href="book.html"' book.html && grep -q 'href="basketball.html".*text-blue-600 font-semibold\|text-blue-600 font-semibold.*href="basketball.html"' basketball.html && grep -q 'href="contact.html".*text-blue-600 font-semibold\|text-blue-600 font-semibold.*href="contact.html"' contact.html && grep -q 'aria-label="Open navigation menu"' index.html && echo "PASS: Active nav + aria-label applied" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html: the desktop `<a href="index.html"` nav link (the "Home" link, not the logo) has class containing `text-blue-600 font-semibold`
    - index.html: the mobile `<a href="index.html"` nav link has class containing `text-blue-600` and does NOT contain `text-gray-800`
    - book.html: the desktop `<a href="book.html"` nav link has class containing `text-blue-600 font-semibold`
    - book.html: the mobile `<a href="book.html"` nav link has class containing `text-blue-600` and does NOT contain `text-gray-800`
    - basketball.html: the desktop `<a href="basketball.html"` nav link has class containing `text-blue-600 font-semibold`
    - basketball.html: the mobile `<a href="basketball.html"` nav link has class containing `text-blue-600` and does NOT contain `text-gray-800`
    - contact.html: the desktop `<a href="contact.html"` nav link has class containing `text-blue-600 font-semibold`
    - contact.html: the mobile `<a href="contact.html"` nav link has class containing `text-blue-600` and does NOT contain `text-gray-800`
    - All 4 pages: non-active desktop nav links still have `text-gray-600 hover:text-blue-600`
    - All 4 pages: `<button id="mobile-menu-btn"` contains `aria-label="Open navigation menu"`
  </acceptance_criteria>
  <done>Every page has its own nav link highlighted with text-blue-600 font-semibold (desktop) and text-blue-600 (mobile). All non-active links retain their inactive styling. Mobile menu button has aria-label on all pages.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full fidelity verification suite:

```bash
cd "/Users/nathanrubin/Downloads/nathan website/Upload"

# Section IDs in correct files
grep -q 'id="hero"' index.html && echo "PASS: #hero" || echo "FAIL: #hero"
grep -q 'id="about"' index.html && echo "PASS: #about" || echo "FAIL: #about"
grep -q 'id="book"' book.html && echo "PASS: #book" || echo "FAIL: #book"
grep -q 'id="events"' book.html && echo "PASS: #events" || echo "FAIL: #events"
grep -q 'id="purchase"' book.html && echo "PASS: #purchase" || echo "FAIL: #purchase"
grep -q 'id="basketball"' basketball.html && echo "PASS: #basketball" || echo "FAIL: #basketball"
grep -q '<form' contact.html && echo "PASS: contact form" || echo "FAIL: contact form"

# Key Imgur URLs
grep -q 'i.imgur.com/hnJz5s5' index.html && echo "PASS: hero bg" || echo "FAIL: hero bg"
grep -q 'i.imgur.com/MEED7S6' book.html && echo "PASS: book cover" || echo "FAIL: book cover"
grep -q 'i.imgur.com/4QRYvm7' basketball.html && echo "PASS: basketball photo" || echo "FAIL: basketball photo"
grep -q 'i.imgur.com/sAcG2HQ' contact.html && echo "PASS: contact logo" || echo "FAIL: contact logo"

# styles.css linked
grep -q 'href="styles.css"' index.html && echo "PASS: styles link index" || echo "FAIL"
grep -q 'href="styles.css"' book.html && echo "PASS: styles link book" || echo "FAIL"
grep -q 'href="styles.css"' basketball.html && echo "PASS: styles link basketball" || echo "FAIL"
grep -q 'href="styles.css"' contact.html && echo "PASS: styles link contact" || echo "FAIL"

# Active nav indicators
grep -q 'text-blue-600 font-semibold' index.html && echo "PASS: active nav index" || echo "FAIL"
grep -q 'text-blue-600 font-semibold' book.html && echo "PASS: active nav book" || echo "FAIL"
grep -q 'text-blue-600 font-semibold' basketball.html && echo "PASS: active nav basketball" || echo "FAIL"
grep -q 'text-blue-600 font-semibold' contact.html && echo "PASS: active nav contact" || echo "FAIL"

# aria-label
grep -q 'aria-label="Open navigation menu"' index.html && echo "PASS: aria index" || echo "FAIL"
grep -q 'aria-label="Open navigation menu"' book.html && echo "PASS: aria book" || echo "FAIL"
grep -q 'aria-label="Open navigation menu"' basketball.html && echo "PASS: aria basketball" || echo "FAIL"
grep -q 'aria-label="Open navigation menu"' contact.html && echo "PASS: aria contact" || echo "FAIL"
```

All checks must output PASS.
</verification>

<success_criteria>
- styles.css exists at repo root with all shared CSS rules
- All 4 HTML pages link to styles.css (no inline shared CSS remains)
- index.html retains hero-specific CSS in a residual inline style block
- Each page's own nav link is visually active with text-blue-600 font-semibold
- Mobile menu button has aria-label on all 4 pages
- All section IDs, Imgur URLs, and content remain in the correct files
- No href values changed on any nav link
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-split-navigation/02-01-SUMMARY.md`
</output>
