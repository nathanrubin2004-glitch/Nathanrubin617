---
phase: 02-page-split-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - SITE-05
  - NAV-01
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "All original Imgur URLs are present in their correct files"
    - "All section IDs are present in their correct files"
    - "Nav links work correctly across all 4 pages"
    - "Visual rendering matches the pre-extraction state"
  artifacts: []
  key_links: []
---

<objective>
Verify content fidelity and visual correctness after CSS extraction and nav changes.

Purpose: Confirm that the CSS extraction and active nav edits did not break any content, images, styles, or navigation (per D-06 and SITE-05).
Output: Verified site with all grep checks passing and visual confirmation from user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-page-split-navigation/02-CONTEXT.md
@.planning/phases/02-page-split-navigation/02-RESEARCH.md
@.planning/phases/02-page-split-navigation/02-UI-SPEC.md
@.planning/phases/02-page-split-navigation/02-01-SUMMARY.md

@styles.css
@index.html
@book.html
@basketball.html
@contact.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive content fidelity grep checks</name>
  <files></files>
  <read_first>
    - index.html (verify section IDs and Imgur URLs are present)
    - book.html (verify section IDs and all 11 Imgur URLs)
    - basketball.html (verify section ID and Imgur URL)
    - contact.html (verify form presence and logo URL)
    - styles.css (verify shared CSS rules are complete)
    - .planning/phases/02-page-split-navigation/02-RESEARCH.md (Fidelity Grep Commands section — use these exact patterns)
    - .planning/phases/02-page-split-navigation/02-UI-SPEC.md (Content Fidelity Checklist — complete Imgur URL table)
  </read_first>
  <action>
    Run the full content fidelity verification suite per D-06 (grep checks, not visual review). Execute ALL of the following checks and report results:

    **Section IDs (7 checks):**
    - `grep -q 'id="hero"' index.html` — PASS/FAIL
    - `grep -q 'id="about"' index.html` — PASS/FAIL
    - `grep -q 'id="book"' book.html` — PASS/FAIL
    - `grep -q 'id="events"' book.html` — PASS/FAIL
    - `grep -q 'id="purchase"' book.html` — PASS/FAIL
    - `grep -q 'id="basketball"' basketball.html` — PASS/FAIL
    - `grep -q '<form' contact.html` — PASS/FAIL

    **ALL Imgur URLs (16 checks — every URL from the UI-SPEC Content Fidelity Checklist):**
    - `grep -q 'i.imgur.com/hnJz5s5' index.html` — hero bg
    - `grep -q 'i.imgur.com/sAcG2HQ' index.html` — NR logo (index)
    - `grep -q 'i.imgur.com/ye3HNgv' index.html` — profile pic
    - `grep -q 'i.imgur.com/MEED7S6' book.html` — book cover
    - `grep -q 'i.imgur.com/28kGjtN' book.html` — Mildred Ave reading
    - `grep -q 'i.imgur.com/bVwdW5V' book.html` — event photo
    - `grep -q 'i.imgur.com/nuZ2lwN' book.html` — event photo
    - `grep -q 'i.imgur.com/GFf1Obh' book.html` — Nathan reading
    - `grep -q 'i.imgur.com/Mdi5D0j' book.html` — Nathan signing
    - `grep -q 'i.imgur.com/vE7tyBu' book.html` — community event
    - `grep -q 'i.imgur.com/rY93SCx' book.html` — Back-2-School
    - `grep -q 'i.imgur.com/TIvy35r' book.html` — Nathan speaking
    - `grep -q 'i.imgur.com/C79Mro6' book.html` — Nathan at SNUG
    - `grep -q 'i.imgur.com/WLelN9A' book.html` — video
    - `grep -q 'i.imgur.com/4QRYvm7' basketball.html` — basketball photo
    - `grep -q 'i.imgur.com/sAcG2HQ' contact.html` — NR logo (contact)

    **styles.css completeness (4 checks):**
    - `grep -q '\-\-color-primary: #3b82f6' styles.css` — CSS variables present
    - `grep -q 'nav-glass' styles.css` — glassmorphism rule present
    - `grep -q 'webkit-scrollbar' styles.css` — scrollbar rules present
    - `grep -c 'chat' styles.css` — chat widget styles present (count > 0)

    **Active nav indicators (4 checks):**
    - `grep 'href="index.html"' index.html | grep -q 'text-blue-600 font-semibold'` — Home active on index
    - `grep 'href="book.html"' book.html | grep -q 'text-blue-600 font-semibold'` — Book active on book
    - `grep 'href="basketball.html"' basketball.html | grep -q 'text-blue-600 font-semibold'` — Basketball active on basketball
    - `grep 'href="contact.html"' contact.html | grep -q 'text-blue-600 font-semibold'` — Contact active on contact

    **Cross-check: no active class on wrong pages (4 spot checks):**
    - On index.html, the book.html link should NOT have `text-blue-600 font-semibold`
    - On book.html, the index.html link should NOT have `text-blue-600 font-semibold`
    - On basketball.html, the contact.html link should NOT have `text-blue-600 font-semibold`
    - On contact.html, the basketball.html link should NOT have `text-blue-600 font-semibold`

    **styles.css link and load order (4 checks):**
    - All 4 pages contain `href="styles.css"`
    - book.html, basketball.html, contact.html do NOT contain `<style>` tags
    - index.html `<style>` block contains `hero-bg` but NOT `nav-glass`

    If ANY check fails, report which ones and attempt to fix.
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && PASS=0 && FAIL=0 && for check in 'grep -q id="hero" index.html' 'grep -q id="about" index.html' 'grep -q id="book" book.html' 'grep -q id="events" book.html' 'grep -q id="basketball" basketball.html' 'grep -q "<form" contact.html' 'grep -q i.imgur.com/hnJz5s5 index.html' 'grep -q i.imgur.com/MEED7S6 book.html' 'grep -q i.imgur.com/4QRYvm7 basketball.html' 'grep -q href=\"styles.css\" index.html' 'grep -q href=\"styles.css\" book.html' 'grep -q href=\"styles.css\" basketball.html' 'grep -q href=\"styles.css\" contact.html' 'grep -q text-blue-600.font-semibold index.html' 'grep -q text-blue-600.font-semibold book.html' 'grep -q text-blue-600.font-semibold basketball.html' 'grep -q text-blue-600.font-semibold contact.html'; do eval "$check" && PASS=$((PASS+1)) || FAIL=$((FAIL+1)); done && echo "PASS: $PASS  FAIL: $FAIL" && test $FAIL -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - All 7 section ID checks pass
    - All 16 Imgur URL checks pass
    - All 4 active nav checks pass
    - All 4 cross-checks pass (no wrong-page active links)
    - All 4 styles.css link checks pass
    - styles.css completeness checks pass (variables, nav-glass, scrollbar, chat styles)
    - book.html, basketball.html, contact.html have zero `<style>` tags
    - index.html `<style>` contains `hero-bg`, does NOT contain `nav-glass`
  </acceptance_criteria>
  <done>All grep-based content fidelity checks pass. Every Imgur URL, section ID, and nav indicator is confirmed in the correct file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 4 pages</name>
  <files></files>
  <action>
    Present the user with the verification checklist below and wait for their response. This is a human visual verification checkpoint — no code changes are made in this task.

    **What was built:** Shared CSS extraction into styles.css, active nav indicators on all pages, aria-label on mobile menu buttons. All automated grep checks have passed confirming content fidelity.

    **How to verify:**

    1. Open index.html in a browser — verify:
       - Hero background image loads (dark overlay visible)
       - "Home" nav link is blue and semibold (other links are gray)
       - Glassmorphism nav bar effect is visible (frosted glass)
       - Profile picture and NR logo are visible
       - All text reads the same as before

    2. Open book.html — verify:
       - "Chasing a Dream" nav link is blue and semibold
       - Book cover image loads
       - All event photos load (scroll through the gallery)
       - Video embed is visible
       - "Get the Book" CTA button is blue (not affected by nav changes)

    3. Open basketball.html — verify:
       - "Basketball" nav link is blue and semibold
       - Basketball photo loads

    4. Open contact.html — verify:
       - "Contact" nav link is blue and semibold
       - Contact form is visible
       - NR logo in footer/header loads

    5. On any page, resize browser to mobile width — verify:
       - Hamburger menu appears
       - Tap hamburger — mobile menu opens
       - Current page link is blue in mobile menu
       - Other links are dark gray

    6. Navigate between all 4 pages — verify:
       - All nav links work
       - Active indicator changes correctly on each page

    **Resume signal:** Type "approved" if all pages look correct, or describe any visual issues you see.
  </action>
  <verify>User confirms visual correctness by typing "approved"</verify>
  <acceptance_criteria>
    - User confirms hero background image loads on index.html
    - User confirms active nav indicator is visible and correct on each page
    - User confirms glassmorphism nav effect is present
    - User confirms mobile hamburger menu works with active indicator
    - User confirms all images load on all pages
  </acceptance_criteria>
  <done>User has visually verified all 4 pages render correctly with styles, images, navigation, and active indicators working as expected.</done>
</task>

</tasks>

<verification>
All automated fidelity checks from Task 1 pass AND user visually confirms all 4 pages render correctly.
</verification>

<success_criteria>
- Zero FAIL results from content fidelity grep suite
- User confirms all 4 pages render correctly with images, styles, and nav working
- Active nav indicator visible on the correct link per page
- Mobile nav functions correctly with active indicator
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-split-navigation/02-02-SUMMARY.md`
</output>
