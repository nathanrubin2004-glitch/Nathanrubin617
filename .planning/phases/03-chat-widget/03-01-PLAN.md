---
phase: 03-chat-widget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - styles.css
  - index.html
  - book.html
  - basketball.html
  - contact.html
autonomous: true
requirements:
  - CHAT-01
  - CHAT-16
  - CHAT-17
  - CHAT-18

must_haves:
  truths:
    - "Chat bubble is visible bottom-right on all 4 pages with iOS safe-area offset"
    - "Chat panel is true full-width on mobile (edge-to-edge, no side margins)"
    - "Chat input container has iOS safe-area bottom padding"
    - "All 4 pages have viewport-fit=cover meta tag enabling safe-area env variables"
  artifacts:
    - path: "styles.css"
      provides: "Updated mobile layout, iOS safe-area insets on bubble and input container"
      contains: "env(safe-area-inset-bottom)"
    - path: "index.html"
      provides: "viewport-fit=cover meta tag"
      contains: "viewport-fit=cover"
    - path: "book.html"
      provides: "viewport-fit=cover meta tag"
      contains: "viewport-fit=cover"
    - path: "basketball.html"
      provides: "viewport-fit=cover meta tag"
      contains: "viewport-fit=cover"
    - path: "contact.html"
      provides: "viewport-fit=cover meta tag"
      contains: "viewport-fit=cover"
  key_links:
    - from: "styles.css"
      to: "all 4 HTML pages"
      via: "link rel=stylesheet"
      pattern: "styles\\.css"
---

<objective>
Update CSS for mobile full-width chat panel, iOS safe-area insets, and viewport meta tags on all pages.

Purpose: The chat panel must feel like a native bottom sheet on mobile (edge-to-edge) and must not overlap iOS browser chrome. These CSS changes are prerequisites for the chat widget to meet CHAT-17 and CHAT-18.

Output: Updated `styles.css` with mobile layout and safe-area rules; all 4 HTML pages with `viewport-fit=cover` meta tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chat-widget/03-CONTEXT.md
@.planning/phases/03-chat-widget/03-UI-SPEC.md

<interfaces>
<!-- Key CSS classes already defined in styles.css that this plan modifies -->

From styles.css (lines 35-38):
```css
.chat-bubble {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
```

From styles.css (lines 205-211):
```css
.chat-input-container {
    border-top: 1px solid #e5e7eb;
    padding: 1rem;
    background: white;
    display: flex;
    gap: 0.75rem;
}
```

From styles.css (lines 251-262) — to be REPLACED:
```css
@media (max-width: 640px) {
    .chat-window {
        max-width: calc(100% - 2rem);
        height: 500px;
        bottom: 6rem;
        right: 1rem;
    }

    .message-bubble {
        max-width: 90%;
    }
}
```

Current viewport meta on all 4 pages (line 6):
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update styles.css — mobile full-width layout and iOS safe-area insets</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (full file — see current mobile media query lines 251-262, .chat-bubble lines 35-53, .chat-input-container lines 205-211)
    - .planning/phases/03-chat-widget/03-UI-SPEC.md (Mobile Layout Contract section and iOS Safe-Area Insets section)
  </read_first>
  <action>
Make three targeted edits to styles.css:

1. **Update `.chat-bubble` bottom value** (line 37) — change from `bottom: 2rem;` to:
   ```css
   bottom: calc(2rem + env(safe-area-inset-bottom));
   ```
   This ensures the floating bubble does not overlap iOS browser chrome. `env(safe-area-inset-bottom)` evaluates to 0 on non-iOS devices.

2. **Update `.chat-input-container` padding** (line 207) — change from `padding: 1rem;` to:
   ```css
   padding-bottom: calc(1rem + env(safe-area-inset-bottom));
   padding-top: 1rem;
   padding-left: 1rem;
   padding-right: 1rem;
   ```
   This prevents the text input from being hidden behind the iOS home indicator bar.

3. **Replace the entire `@media (max-width: 640px)` block** (lines 251-262) with:
   ```css
   @media (max-width: 640px) {
       .chat-window {
           width: 100%;
           max-width: 100%;
           left: 0;
           right: 0;
           bottom: 0;
           height: 70vh;
           border-radius: 12px 12px 0 0;
       }

       .message-bubble {
           max-width: 90%;
       }
   }
   ```
   Per D-05: true full-width (edge-to-edge, no side margins), flat bottom, 12px rounded top corners, 70vh height for more screen real estate.

Do NOT change any other CSS rules. All other `.chat-*` classes remain untouched.
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && grep -c "env(safe-area-inset-bottom)" styles.css | grep -q "2" && grep "width: 100%" styles.css | grep -q "100%" && grep "height: 70vh" styles.css | grep -q "70vh" && grep "border-radius: 12px 12px 0 0" styles.css | grep -q "12px" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains exactly 2 occurrences of `env(safe-area-inset-bottom)` — one in `.chat-bubble`, one in `.chat-input-container`
    - styles.css `.chat-bubble` has `bottom: calc(2rem + env(safe-area-inset-bottom))`
    - styles.css `.chat-input-container` has `padding-bottom: calc(1rem + env(safe-area-inset-bottom))`
    - styles.css `@media (max-width: 640px)` `.chat-window` has `width: 100%;`, `max-width: 100%;`, `left: 0;`, `right: 0;`, `bottom: 0;`, `height: 70vh;`, `border-radius: 12px 12px 0 0;`
    - styles.css `@media (max-width: 640px)` `.message-bubble` has `max-width: 90%;`
    - No other CSS rules outside `.chat-bubble`, `.chat-input-container`, and the mobile media query were changed
  </acceptance_criteria>
  <done>Mobile chat panel is true full-width with rounded top corners; iOS safe-area insets protect bubble and input from overlapping browser chrome.</done>
</task>

<task type="auto">
  <name>Task 2: Update viewport meta tag on all 4 HTML pages</name>
  <files>index.html, book.html, basketball.html, contact.html</files>
  <read_first>
    - index.html (line 6 — current viewport meta tag)
    - book.html (line 6)
    - basketball.html (line 6)
    - contact.html (line 6)
  </read_first>
  <action>
On each of the 4 HTML pages, replace the viewport meta tag (line 6 in each file):

**Before:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

**After:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
```

This activates `env(safe-area-inset-bottom)` on iOS Safari. Without `viewport-fit=cover`, the env() values are always 0 even on iOS devices. The change from `1.0` to `1` is cosmetic (equivalent values).

Do NOT change any other content in these files. Only the meta viewport tag on line 6.
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && for f in index.html book.html basketball.html contact.html; do grep -q "viewport-fit=cover" "$f" || { echo "FAIL: $f missing viewport-fit=cover"; exit 1; }; done && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `viewport-fit=cover` in the viewport meta tag
    - book.html contains `viewport-fit=cover` in the viewport meta tag
    - basketball.html contains `viewport-fit=cover` in the viewport meta tag
    - contact.html contains `viewport-fit=cover` in the viewport meta tag
    - All 4 meta tags read exactly: `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`
  </acceptance_criteria>
  <done>All 4 pages have viewport-fit=cover enabling iOS safe-area environment variables.</done>
</task>

</tasks>

<verification>
1. `grep -c "env(safe-area-inset-bottom)" styles.css` returns `2`
2. `grep "viewport-fit=cover" index.html book.html basketball.html contact.html` returns 4 matches
3. `grep "width: 100%" styles.css` appears in the mobile media query
4. `grep "height: 70vh" styles.css` appears in the mobile media query
5. `grep "border-radius: 12px 12px 0 0" styles.css` appears in the mobile media query
</verification>

<success_criteria>
- Mobile chat panel is full-width (edge-to-edge) with 70vh height and rounded top corners
- iOS safe-area insets applied to chat bubble bottom and input container padding
- All 4 pages activate safe-area env variables via viewport-fit=cover
- No visual regressions on desktop (env() evaluates to 0 on non-iOS)
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-widget/03-01-SUMMARY.md`
</output>
