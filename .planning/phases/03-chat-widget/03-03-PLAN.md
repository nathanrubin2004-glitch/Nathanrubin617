---
phase: 03-chat-widget
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-06
  - CHAT-07
  - CHAT-08
  - CHAT-09
  - CHAT-10
  - CHAT-11
  - CHAT-12
  - CHAT-13
  - CHAT-15
  - CHAT-16
  - CHAT-17
  - CHAT-18

must_haves:
  truths:
    - "Chat bubble visible bottom-right on all 4 pages"
    - "Chat panel opens and closes on click"
    - "Greeting message appears on first open"
    - "Visitor can send message and receive reply"
    - "Typing indicator appears while waiting"
    - "Error message appears if API fails"
    - "Mobile panel is full-width edge-to-edge"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete chat widget works end-to-end on all 4 pages via local dev server.

Purpose: Confirm that JS extraction, CSS changes, and behavioral improvements all work together before deploying.

Output: Verification results confirming all Phase 3 requirements are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chat-widget/03-CONTEXT.md
@.planning/phases/03-chat-widget/03-01-SUMMARY.md
@.planning/phases/03-chat-widget/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated grep verification of all Phase 3 deliverables</name>
  <files></files>
  <read_first>
    - chat.js (verify full file contents)
    - styles.css (verify CSS changes)
    - index.html (verify script tag and no inline JS)
    - book.html (verify script tag and no inline JS)
    - basketball.html (verify script tag and no inline JS)
    - contact.html (verify script tag and no inline JS)
  </read_first>
  <action>
Run a comprehensive grep-based verification of all Phase 3 deliverables. Check every requirement:

**CHAT-01**: `grep 'id="chat-bubble"' index.html book.html basketball.html contact.html` — 4 matches
**CHAT-02**: `grep 'classList.toggle.*active' chat.js` — bubble click toggles panel
**CHAT-03**: `grep 'classList.remove.*active' chat.js` — close button removes active
**CHAT-04**: `grep "I'm Nathan's AI assistant" chat.js` — greeting present
**CHAT-06**: `grep 'sendMessage' chat.js` — send on click and Enter
**CHAT-07**: `grep 'chatSend.disabled = true' chat.js` — input locking
**CHAT-08**: `grep 'showTypingIndicator\|removeTypingIndicator' chat.js` — typing indicator functions
**CHAT-09**: `grep 'message-assistant.*message-bubble\|message-user.*message-bubble' chat.js` — distinct bubbles
**CHAT-10**: `grep 'message-user\|message-assistant' chat.js` — different classes
**CHAT-11**: `grep 'scrollTop.*scrollHeight' chat.js` — auto-scroll
**CHAT-12**: `grep 'conversationHistory' chat.js` — session persistence
**CHAT-13**: `grep 'Something went wrong' chat.js` — error message (2 occurrences)
**CHAT-15**: `grep 'src="chat.js"' index.html book.html basketball.html contact.html` — shared file loaded by all 4
**CHAT-16**: `grep '#3b82f6' styles.css` — blue color scheme present
**CHAT-17**: `grep 'width: 100%' styles.css` — full-width mobile
**CHAT-18**: `grep 'env(safe-area-inset-bottom)' styles.css` — iOS safe-area (2 occurrences)

Also verify NO regressions:
- `grep 'mobile-menu-btn' index.html book.html basketball.html contact.html` — mobile menu preserved (4 matches)
- `grep '// Chat Widget Logic' index.html book.html basketball.html contact.html` — inline JS removed (0 matches)
- `grep 'viewport-fit=cover' index.html book.html basketball.html contact.html` — viewport meta (4 matches)

Report results as a checklist. If any check fails, document what is missing and fix it.
  </action>
  <verify>
    <automated>cd "/Users/nathanrubin/Downloads/nathan website/Upload" && PASS=true; grep -q 'showTypingIndicator' chat.js || PASS=false; grep -q 'removeTypingIndicator' chat.js || PASS=false; grep -c 'Something went wrong' chat.js | grep -q '2' || PASS=false; grep -q "I'm Nathan's AI assistant" chat.js || PASS=false; grep -q 'env(safe-area-inset-bottom)' styles.css || PASS=false; grep -q 'width: 100%' styles.css || PASS=false; for f in index.html book.html basketball.html contact.html; do grep -q 'src="chat.js"' "$f" || PASS=false; grep -q 'viewport-fit=cover' "$f" || PASS=false; grep -q 'mobile-menu-btn' "$f" || PASS=false; grep -q '// Chat Widget Logic' "$f" && PASS=false; done; $PASS && echo "ALL CHECKS PASS" || echo "SOME CHECKS FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - All 16 requirement checks pass (CHAT-01 through CHAT-18 minus CHAT-05 and CHAT-14)
    - No inline chat JS remains in any HTML file
    - Mobile menu toggle preserved in all 4 HTML files
    - viewport-fit=cover present in all 4 HTML files
  </acceptance_criteria>
  <done>All automated checks pass, confirming Phase 3 deliverables are complete and correct.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of chat widget end-to-end</name>
  <files></files>
  <action>
Present the user with verification steps to manually test the chat widget on the local dev server. The user will run `npx netlify-cli dev` and test in-browser.
  </action>
  <verify>User confirms all checks pass by typing "approved".</verify>
  <done>User has approved the chat widget functionality.</done>
  <what-built>Complete chat widget with shared JS, typing indicator, updated greeting, mobile full-width layout, and iOS safe-area insets — live on all 4 pages.</what-built>
  <how-to-verify>
    1. Run `npx netlify-cli dev` from the project root to start the local dev server
    2. Open http://localhost:8888/index.html in your browser

    **Test on index.html first, then spot-check one other page (e.g., book.html):**

    a. CHAT-01: Confirm blue chat bubble visible in bottom-right corner
    b. CHAT-02: Click the bubble — chat panel should open
    c. CHAT-04: Greeting message should appear: "Hi! ... I'm Nathan's AI assistant..."
    d. CHAT-06: Type "Tell me about Nathan's book" and press Enter (or click send arrow)
    e. CHAT-07: Send button should turn gray and input should be locked while waiting
    f. CHAT-08: Three bouncing dots should appear while waiting for the reply
    g. CHAT-09/10: Reply should appear as a gray bubble on the left; your message is blue on the right
    h. CHAT-11: Panel should auto-scroll to show the latest message
    i. CHAT-03: Click the X to close the panel, then click the bubble to reopen — messages should still be there (CHAT-12)
    j. CHAT-13: (Optional) Temporarily break the API URL in chat.js to test error — should show "Something went wrong — try again"

    **Mobile test:**
    k. CHAT-17: Open Chrome DevTools, toggle device toolbar (Ctrl+Shift+M), select iPhone 12 — chat panel should be edge-to-edge full-width with rounded top corners

    3. Open book.html — verify bubble is present and chat works there too
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues you see.</resume-signal>
</task>

</tasks>

<verification>
All Phase 3 requirements verified via automated grep checks and manual human testing.
</verification>

<success_criteria>
- All 16 CHAT requirements assigned to Phase 3 are verified
- User confirms chat widget works end-to-end on at least 2 pages
- No visual regressions on desktop or mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-chat-widget/03-03-SUMMARY.md`
</output>
